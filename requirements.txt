pyserial>=3.5
pyqtgraph>=0.13
# Qt bindings are not guaranteed via pip on ARMv7.
# On Windows / x86_64, install PySide6>=6.5.
# On Raspberry Pi OS 32-bit, install a system Qt package such as
# python3-pyqt5 or python3-pyside2 instead of PySide6.
